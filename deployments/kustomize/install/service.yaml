kind: Service
apiVersion: v1
metadata:
  name: vikyb-ambulance-webapi
spec:
  selector:
    pod: vikyb-ambulance-webapi-label
  ports:
  - name: http
    protocol: TCP
    port: 80
    targetPort: webapi-port